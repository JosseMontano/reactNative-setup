import { StyleSheet, Text, View, Image } from 'react-native';
type ParamsType = {
}
export const Register = ({}:ParamsType) => {
    return (
        <View>
            <Text>register</Text>
        </View>
    );
}
const styles = StyleSheet.create({

});